@model IEnumerable<GestionJuridico.Models.Accion>
@{
    ViewData["Title"] = "Acciones";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="col-3">
                    <a asp-action="Create" class="btn btn-primary btn-block"><i class="fa fa-plus-circle"></i> Nueva acción</a>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card card-gray-dark">
            <div class="card-header">
                <h3 class="card-title">Lista</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <table id="defaultTable" class="table table-bordered table-hover">
                    <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Codigo)</th>
                        <th>@Html.DisplayNameFor(model => model.Nombre)</th>
                        <th>@Html.DisplayNameFor(model => model.EstadoActual)</th>
                        <th>@Html.DisplayNameFor(model => model.EstadoSiguiente)</th>
                        <th>@Html.DisplayNameFor(model => model.EstadoActual.Proceso)</th>
                        <th>Acciones</th>
                    </tr>
                    </thead>
                    <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Codigo)</td>
                            <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.EstadoActual.Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.EstadoSiguiente.Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.EstadoActual.Proceso.Nombre)</td>
                            <td>
                                <div class="btn-group">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-default">
                                        <i class="fa fa-eye"></i>
                                    </a>
                                    <button type="button" value="@item.Id" data-item="@item.Nombre" class="btn btn-danger btn-delete" title="Eliminar">
                                        <i class="fa fa-trash"></i>
                                    </button>
                                    <form asp-action="Delete"></form>
                                </div>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>

@section Scripts
{
    <script>
        // Variable para seleccionar columnas para buscar
        var customColumnSearch = [0,1,2,3,4]
    </script>
    <partial name="_IndexJSPartial"/>
}